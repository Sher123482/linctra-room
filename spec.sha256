2140d80f204953ab948e6fd98ef1fb5399c408ebfdfad95f1009145affb07ad2

# 策略規則承諾 (salted SHA-256 commitment)
# committed_at: 2026-08-17T06:42:23Z
#
# 公開揭露：三刀流均線策略，黃金/死亡交叉進場。其餘規則與全部參數未公開。
#
# 上面那串雜湊涵蓋了兩條腿的完整設定（含所有參數數值）與一段隨機鹽值。
# 它不洩漏任何參數，但鎖死了規則：日後公開 salt 與設定即可驗證
# 「這確實是 2026-08-17 當天承諾的那一套」。
#
# 驗證方式（日後揭露時）：
#   sha256( json.dumps(spec, sort_keys=True, separators=(',',':'),
#                      ensure_ascii=False).encode('utf-8') )
#
#   編碼必須是 UTF-8。設定裡含非 ASCII 字元，換成其他編碼（UTF-16、
#   帶 BOM 的 UTF-8 等）會算出完全不同的雜湊，比對就會失敗。
#
#   把 spec-private.json 與本檔放在同一層，執行下面這行即可自行驗證
#   （複製時記得去掉開頭的「# 」）：
#
# python -c "import json,hashlib;print(hashlib.sha256(json.dumps(json.load(open('spec-private.json',encoding='utf-8')),sort_keys=True,separators=(',',':'),ensure_ascii=False).encode('utf-8')).hexdigest())"
#
#   輸出應該等於本檔第一行。
