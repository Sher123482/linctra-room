# 白名單式忽略：預設忽略所有東西，只放行房間發佈需要的檔案。
# 目的是結構性防止策略程式碼、參數、金鑰被誤commit進這個「公開」repo。
*

!.gitignore
!README.md
!map.json
!logo.webp

# 權益曲線圖，掛在房間牆上（由本機工具產生）
!chart-*.png

# 策略規則的加鹽承諾。只有雜湊值可以公開。
!spec.sha256

# 已平倉交易紀錄。目錄要先放行，git 才會進去看裡面的檔案。
!signals/
!signals/*.json

# 每月狀態存證。attest.py 刻意放在根目錄而不是 tools/：
# 它要在 GitHub Actions 上執行所以必須公開，而 tools/ 的原則是「一律不公開」。
# 公開的程式放公開區，界線才不會被開洞。
!attest.py
!attestations.jsonl
!.github/
!.github/workflows/
!.github/workflows/*.yml

# spec-private.json 含 salt 與完整參數，絕不上傳。
# 上面的 * 已經擋住它了，這裡明寫一次是為了讓意圖清楚，
# 也防止日後有人改動白名單時誤放行。
spec-private.json
